# Published content is bilingual from the start

Public content will store explicit `pt-BR` and `en-US` versions, and both are required before an admin save can publish. V1 will index only the canonical `pt-BR` material for retrieval and answer in the visitor-selected language, avoiding duplicated indexes while preserving deliberate English portfolio copy.
